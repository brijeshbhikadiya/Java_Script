//Check whether a number is even or odd

var number = -1;

if(number % 2 === 0)  //== : compare to variable but it ignores the data type.
                     //=== : compare to variable but it doesn't ignores the data type.
{
    console.log("Given Number is Even");
}
else
{
    console.log("Given number is Odd");
}
